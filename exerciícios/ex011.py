# Pintura da Parede
a = float(input('Digite a altura da parede: '))
l = float(input('Digite a largura da parede: '))

print('Esta parede tem {} m² de área e necessita de {:.2f} litros de tinta.'.format(a*l, (a*l)/2))
